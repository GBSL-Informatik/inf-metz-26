---
draft: true
page_id: 50eea9a7-4dba-4526-b7b4-7d5649352c0a
---

import Struktogramm from "@tdev-components/Struktogramm";

# 🔒 Interner Playground
Zur Erstellung von Struktogrammen für Arbeitsblätter und Folien.

## Probe

### n!

<Struktogramm program={[
  {type: 'input', code: <span>Eingabe: <span className="var">n</span></span>},
  {type: 'step', code: <span><span className="var">erg</span> = 1</span>},

  {type: 'repeat', code: <span>Solange <span className="var">n</span> &gt; 1</span>, block: [
      {type: 'step', code: <span><span className="var">erg</span> = <span className="var">erg</span> * <span className="var">n</span></span>},
      {type: 'step', code: <span><span className="var">n</span> = <span className="var">n</span> - 1</span>},
    ]},

  {type: 'output', code: <span>Ausgabe: <span className="var">erg</span></span>},
]} />

### Quersumme
<Struktogramm program={[
  {type: 'input', code: <span>Eingabe: <span className="var">n</span></span>},
  {type: 'step', code: <span><span className="var">summe</span> = 0</span>},

  {type: 'repeat', code: <span>Solange <span className="var">n</span> &gt; 0</span>, block: [
      {type: 'step', code: <span><span className="var">ziffer</span> = <span className="var">n</span> mod 10</span>},
      {type: 'step', code: <span><span className="var">summe</span> = <span className="var">summe</span> + <span className="var">ziffer</span></span>},
      {type: 'step', code: <span><span className="var">n</span> = <span className="var">n</span> // 10</span>},
    ]},

  {type: 'output', code: <span>Ausgabe: <span className="var">summe</span></span>},
]} />

### Dezimal -> Binär
<Struktogramm program={[
  {type: 'input', code: <span>Eingabe: <span className="var">n</span></span>},
  {type: 'step', code: <span><span className="var">bin</span> = ""</span>},

  {type: 'if', code: <span>Wenn <span className="var">n</span> = 0:</span>, block: [
      {type: 'output', code: <span>Ausgabe: "0"</span>},
      {type: 'step', code: <span>Ende</span>},
    ]},

  {type: 'repeat', code: <span>Solange <span className="var">n</span> &gt; 0</span>, block: [
      {type: 'step', code: <span><span className="var">rest</span> = <span className="var">n</span> mod 2</span>},
      {type: 'step', code: <span><span className="var">bin</span> = <span className="var">rest</span> + <span className="var">bin</span></span>},
      {type: 'step', code: <span><span className="var">n</span> = <span className="var">n</span> // 2</span>},
    ]},

  {type: 'output', code: <span>Ausgabe: <span className="var">bin</span></span>},
]} />
### Primzahltest
<Struktogramm program={[
  {type: 'input', code: <span>Eingabe: <span className="var">n</span></span>},
  {type: 'step', code: <span><span className="var">isPrime</span> = true</span>},
  {type: 'step', code: <span><span className="var">i</span> = 2</span>},

  {type: 'if', code: <span>Wenn <span className="var">n</span> ≤ 1:</span>, block: [
      {type: 'step', code: <span><span className="var">isPrime</span> = false</span>},
    ]},

  {type: 'repeat', code: <span>Solange <span className="var">i</span> &lt; <span className="var">n</span> UND <span className="var">isPrime</span> = true</span>, block: [
      {type: 'if', code: <span>Wenn <span className="var">n</span> mod <span className="var">i</span> = 0:</span>, block: [
          {type: 'step', code: <span><span className="var">isPrime</span> = false</span>},
        ]},
      {type: 'step', code: <span><span className="var">i</span> = <span className="var">i</span> + 1</span>},
    ]},

  {type: 'if', code: <span>Wenn <span className="var">isPrime</span> = true:</span>, block: [
      {type: 'output', code: <span>Ausgabe: „Primzahl“</span>},
    ]},
  {type: 'if', code: <span>Sonst:</span>, block: [
      {type: 'output', code: <span>Ausgabe: „Keine Primzahl“</span>},
    ]},
]} />
### Ziffern zählen
<Struktogramm program={[
  {type: 'input', code: <span>Eingabe: <span className="var">n</span></span>},

  {type: 'step', code: <span><span className="var">zaehler</span> = 0</span>},

  {type: 'repeat', code: <span>Solange <span className="var">n</span> &gt; 0</span>, block: [

      // QUOTIENT PER SUBTRAKTION ERMITTELN
      {type: 'step', code: <span><span className="var">d</span> = 0</span>},

      {type: 'repeat', code: <span>Solange <span className="var">n</span> ≥ 10</span>, block: [
          {type: 'step', code: <span><span className="var">n</span> = <span className="var">n</span> - 10</span>},
          {type: 'step', code: <span><span className="var">d</span> = <span className="var">d</span> + 1</span>},
      ]},

      // Ziffer entfernt → nächste Runde
      {type: 'step', code: <span><span className="var">zaehler</span> = <span className="var">zaehler</span> + 1</span>},

      // Für nächste Stelle Zahl durch 10 ersetzen (quotient)
      {type: 'step', code: <span><span className="var">n</span> = <span className="var">d</span></span>},
  ]},

  {type: 'output', code: <span>Ausgabe: <span className="var">zaehler</span></span>},
]} />

### Fibonacci-Zahlen

<Struktogramm program={[
  {type: 'input', code: <span>Eingabe: <span className="var">n</span></span>},
  {type: 'step', code: <span><span className="var">zähler</span> = 1</span>},
  {type: 'step', code: <span><span className="var">a</span> = 0</span>},
  {type: 'step', code: <span><span className="var">b</span> = 1</span>},
  {type: 'repeat', code: <span>Solange <span className="var">zähler</span> &lt; <span className="var">n</span></span>, block: [
      {type: 'step', code: <span><span className="var">summe</span> = <span className="var">a</span> + <span className="var">b</span></span>},
      {type: 'step', code: <span><span className="var">a</span> = <span className="var">b</span></span>},
      {type: 'step', code: <span><span className="var">b</span> = <span className="var">summe</span></span>},
      {type: 'step', code: <span><span className="var">zähler</span> = <span className="var">zähler</span> + 1</span>},
      {type: 'output', code: <span>Ausgabe: <span className="var">summe</span></span>},
    ]},
]} />

## AB: Struktogramme analysieren 1
### Aufgabe 1
<Struktogramm program={[
  {type: 'input', code: <span>Eingabe: <span className="var">a</span> und <span className="var">b</span></span>},
  {type: 'step', code: <span><span className="var">c</span> = 0</span>},
  {type: 'repeat', code: <span>Solange <span className="var">a</span> ≥ <span className="var">b</span></span>, block: [
      {type: 'step', code: <span><span className="var">a</span> = <span className="var">a</span> - <span className="var">b</span></span>},
      {type: 'step', code: <span><span className="var">c</span> = <span className="var">c</span> + 1</span>},
    ]},
  {type: 'output', code: <span>Ausgabe: <span className="var">a</span> und <span className="var">c</span></span>},
]} />

### Aufgabe 2
<Struktogramm program={[
  {type: 'input', code: <span>Eingabe: <span className="var">a</span> und <span className="var">b</span></span>},
  {type: 'repeat', code: <span>Solange <span className="var">a</span> ≠ 0</span>, block: [
      {type: 'if', code: <span>Wenn <span className="var">a</span> &lt; <span className="var">b</span>:</span>, block: [
          {type: 'step', code: <span>Vertausche <span className="var">a</span> und <span className="var">b</span></span>},
        ]},
      {type: 'if', code: <span>Sonst:</span>, block: [
          {type: 'step', code: <span><span className="var">a</span> = <span className="var">a</span> - <span className="var">b</span></span>},
        ]},
    ]},
  {type: 'output', code: <span>Ausgabe: <span className="var">b</span></span>},
]} />

## AB: Struktogramme analysieren 2
### Aufgabe 1
<Struktogramm program={[
  {type: 'input', code: <span>Eingabe: <span className="var">Umsatz</span></span>},
  {type: 'if', code: <span>Wenn <span className="var">Umsatz</span> &gt; 100:</span>, block: [
      {type: 'if', code: <span>Wenn <span className="var">Umsatz</span> &gt; 500:</span>, block: [
          {type: 'step', code: <span><span className="var">Rabatt</span> = 10</span>}
        ]},
      {type: 'else', code: <span>Sonst:</span>, block: [
          {type: 'step', code: <span><span className="var">Rabatt</span> = 5</span>}
        ]},
    ]},
  {type: 'else', code: <span>Sonst:</span>, block: [
      {type: 'step', code: <span><span className="var">Rabatt</span> = 0</span>}
    ]},
  {type: 'step', code: <span><span className="var">Rechnungsbetrag</span> = <span className="var">Umsatz</span> -  <span className="var">Rabatt</span></span>},
  {type: 'output', code: <span>Ausgabe: <span className="var">Rechnungsbetrag</span></span>},
]} />

### Aufgabe 2
<Struktogramm program={[
  {type: 'step', code: <span><span className="var">a</span> = 10</span>},
  {type: 'step', code: <span><span className="var">b</span> = 20</span>},
  {type: 'repeat', code: <span>Solange <span className="var">b</span> &gt; 5</span>, block: [
      {type: 'step', code: <span><span className="var">b</span> = <span className="var">b</span> - 5</span>},
      {type: 'step', code: <span><span className="var">a</span> = <span className="var">a</span> + 2</span>},
    ]},
  {type: 'output', code: <span>Ausgabe: <span className="var">a</span></span>},
]} />

### Aufgabe 3
<Struktogramm program={[
  {type: 'input', code: <span>Eingabe <span className="var">K</span></span>},
  {type: 'step', code: <span><span className="var">K1</span> = K</span>},
  {type: 'input', code: <span>Eingabe <span className="var">p</span></span>},
  {type: 'step', code: <span><span className="var">Jahr</span> = 0</span>},
  {type: 'repeat', code: <span>Solange <span className="var">K1</span> &lt; <span className="var">K</span>*2</span>, block: [
      {type: 'step', code: <span><span className="var">K1</span> = <span className="var">K1</span>*(1+<span className="var">p</span>/100)</span>},
      {type: 'step', code: <span><span className="var">Jahr</span> = <span className="var">Jahr</span> + 1</span>},
    ]},
  {type: 'output', code: <span>Ausgabe: <span className="var">Jahr</span></span>},
]} />

### Aufgabe 4
<Struktogramm program={[
  {type: 'step', code: <span><span className="var">Summe</span> = 0</span>},
  {type: 'step', code: <span><span className="var">x</span> = 5</span>},
  {type: 'repeat', code: <span>Solange <span className="var">x</span> &gt; 1</span>, block: [
      {type: 'step', code: <span><span className="var">Summe</span> = <span className="var">Summe</span> + <span className="var">x</span></span>},
      {type: 'if', code: <span>Wenn <span className="var">Summe</span> &gt; 10:</span>, block: [
          {type: 'step', code: <span><span className="var">x</span> = 1</span>}
        ]},
      {type: 'if', code: <span>Sonst:</span>, block: [
          {type: 'step', code: <span><span className="var">x</span> = <span className="var">x</span> - 1</span>}
        ]},
    ]},
  {type: 'output', code: <span>Ausgabe: <span className="var">Summe</span></span>},
]} />

### Aufgabe 5
<Struktogramm program={[
  {type: 'input', code: <span>Eingabe <span className="var">a</span></span>},
  {type: 'input', code: <span>Eingabe <span className="var">b</span></span>},
  {type: 'step', code: <span><span className="var">s</span> = 0</span>},
  {type: 'if', code: <span>Wenn <span className="var">a</span> &gt; <span className="var">b</span>:</span>, block: [
      {type: 'step', code: <span><span className="var">b</span> = <span className="var">a</span> + 1</span>}
    ]},
  {type: 'else', code: <span>Sonst:</span>, block: [
      {type: 'step', code: <span><span className="var">a</span> = <span className="var">a</span> - 1</span>}
    ]},
  {type: 'repeat', code: <span>Solange <span className="var">a</span> &lt; <span className="var">b</span>:</span>, block: [
      {type: 'step', code: <span><span className="var">s</span> = <span className="var">s</span> + <span className="var">b</span></span>}
    ]},
  {type: 'output', code: <span>Ausgabe: <span className="var">b</span></span>},
]} />

### Aufgabe 6
<Struktogramm program={[
  {type: 'input', code: <span>Eingabe <span className="var">a</span></span>},
  {type: 'input', code: <span>Eingabe <span className="var">b</span></span>},
  {type: 'step', code: <span><span className="var">s</span> = 0</span>},
  {type: 'repeat', code: <span>Solange <span className="var">a</span> &gt; <span className="var">b</span>:</span>, block: [
      {type: 'step', code: <span><span className="var">s</span> = <span className="var">s</span> + <span className="var">b</span></span>},
      {type: 'step', code: <span><span className="var">a</span> = <span className="var">a</span> + 1</span>}
    ]},
  {type: 'output', code: <span>Ausgabe: <span className="var">s</span></span>},
]} />

### Quadrat mit while-Schleife
<Struktogramm program={[
  {type: 'step', code: <span><span className="var">zähler</span> = 0</span>},
  {type: 'repeat', code: <span>Solange <span className="var">zähler</span> &lt; 4:</span>, block: [
      {type: 'step', code: <span>forward(50)</span>},
      {type: 'step', code: <span>right(90)</span>},
      {type: 'step', code: <span><span className="var">zähler</span> = <span className="var">zähler</span> + 1</span>}
    ]},
]} />

## Diverses
### Quadrat mit for-Schleife
<Struktogramm program={[
  {type: 'repeat', code: <span>Wiederhole 4x:</span>, block: [
      {type: 'step', code: <span>forward(50)</span>},
      {type: 'step', code: <span>right(90)</span>},
    ]},
]} />

### $a^b$
<Struktogramm program={[
  {type: 'input', code: <span>Eingabe: <span className="var">a</span></span>},
  {type: 'input', code: <span>Eingabe: <span className="var">b</span></span>},
  {type: 'step', code: <span><span className="var">zähler</span> = 0</span>},
  {type: 'step', code: <span><span className="var">resultat</span> = 1</span>},
  {type: 'repeat', code: <span>Solange <span className="var">zähler</span> &lt; <span className="var">b</span>:</span>, block: [
      {type: 'step', code: <span><span className="var">resultat</span> = <span className="var">resultat</span> * <span className="var">a</span></span>},
      {type: 'step', code: <span><span className="var">zähler</span> = <span className="var">zähler</span> + 1</span>},
    ]},
  {type: 'output', code: <span>Ausgabe: <span className="var">resultat</span></span>},
]} />

## Negativbeispiele
### Summe aller Zahlen von 0 bis n (korrekt)
<Struktogramm program={[
  {type: 'input', code: <span>Eingabe: <span className="var">n</span></span>},
  {type: 'step', code: <span><span className="var">summe</span> = 0</span>},
  {type: 'step', code: <span><span className="var">zähler</span> = 0</span>},
  {type: 'repeat', code: <span>Solange <span className="var">zähler</span> &le; <span className="var">n</span>:</span>, block: [
      {type: 'step', code: <span><span className="var">summe</span> = <span className="var">summe</span> + <span className="var">zähler</span></span>},
      {type: 'step', code: <span><span className="var">zähler</span> = <span className="var">zähler</span> + 1</span>},
    ]},
  {type: 'output', code: <span>Ausgabe: <span className="var">summe</span></span>},
]} />

### Summe aller Zahlen von 0 bis n (mit diversen Fehlern)
<Struktogramm program={[
  {type: 'input', code: <span>Eingabe: <span className="var">n</span></span>},
  {type: 'step', code: <span><span className="var">zähler</span> = 0</span>},
  {type: 'repeat', code: <span>Solange <span className="var">zähler</span> &le; <span className="var">n</span>:</span>, block: [
      {type: 'step', code: <span><span className="var">summe</span> + <span className="var">zähler</span></span>},
      {type: 'step', code: <span><span className="var">zähler</span> + 1</span>},
    ]},
  {type: 'output', code: <span>Ausgabe: <span className="var">summe</span></span>},
]} />

### Summe aller Zahlen von 0 bis n (falsche Ausgabe)
<Struktogramm program={[
  {type: 'input', code: <span>Eingabe: <span className="var">n</span></span>},
  {type: 'step', code: <span><span className="var">summe</span> = 0</span>},
  {type: 'step', code: <span><span className="var">zähler</span> = 0</span>},
  {type: 'repeat', code: <span>Solange <span className="var">zähler</span> &le; <span className="var">n</span>:</span>, block: [
      {type: 'step', code: <span><span className="var">summe</span> = <span className="var">summe</span> + <span className="var">zähler</span></span>},
      {type: 'step', code: <span><span className="var">zähler</span> = <span className="var">zähler</span> + 1</span>},
    ]},
  {type: 'output', code: <span>Ausgabe: <span className="var">zähler</span></span>},
]} />

### Summe aller Zahlen von 0 bis n (Eingabe / Ausgabe falsch notiert)
<Struktogramm program={[
  {type: 'input', code: <span>Eingabe = <span className="var">n</span></span>},
  {type: 'step', code: <span><span className="var">summe</span> = 0</span>},
  {type: 'step', code: <span><span className="var">zähler</span> = 0</span>},
  {type: 'repeat', code: <span>Solange <span className="var">zähler</span> &le; <span className="var">n</span>:</span>, block: [
      {type: 'step', code: <span><span className="var">summe</span> = <span className="var">summe</span> + <span className="var">zähler</span></span>},
      {type: 'step', code: <span><span className="var">zähler</span> = <span className="var">zähler</span> + 1</span>},
    ]},
  {type: 'output', code: <span>Ausgabe = 15</span>},
]} />

## Input / Output
<Struktogramm program={[
  {type: 'input', code: <span>Eingabe: <span className="var">n</span></span>},
  {type: 'step', code: <span><span className="var">summe</span> = 0</span>},
  {type: 'step', code: <span><span className="var">zaehler</span> = 0</span>},
  {type: 'repeat', code: <span>Solange <span className="var">zaehler</span> &le; <span className="var">n</span>:</span>, block: [
      {type: 'step', code: <span><span className="var">summe</span> = <span className="var">summe</span> + <span className="var">zaehler</span></span>},
      {type: 'step', code: <span><span className="var">zaehler</span> = <span className="var">zaehler</span> + 1</span>},
    ]},
  {type: 'output', code: <span>Ausgabe: <span className="var">summe</span></span>},
]} />

<Struktogramm program={[
  {type: 'input', code: <span><span className="var">n</span> = input('Zahl eingeben: ')</span>},
  {type: 'step', code: <span><span className="var">summe</span> = 0</span>},
  {type: 'step', code: <span><span className="var">zaehler</span> = 0</span>},
  {type: 'repeat', code: <span>while <span className="var">zaehler</span> &lt;= <span className="var">n</span>:</span>, block: [
      {type: 'step', code: <span><span className="var">summe</span> = <span className="var">summe</span> + <span className="var">zaehler</span></span>},
      {type: 'step', code: <span><span className="var">zaehler</span> = <span className="var">zaehler</span> + 1</span>},
    ]},
  {type: 'output', code: <span>print(<span className="var">summe</span>)</span>},
]} />

## LearningApp zu Struktogrammen
<Struktogramm program={[
  {type: 'step', code: <span><span className="var">Restbetrag</span> = <span className='var'>Preis</span> - <span className='var'>Anzahlung</span></span>}
]} />

<Struktogramm program={[
  {type: 'repeat', code: <span>Solange <span className="var">prozent</span> == 100:</span>, block: [
      {type: 'step', code: <span>Foo</span>},
      {type: 'step', code: <span>Bar</span>}
    ]} 
]} />

<Struktogramm program={[
  {type: 'input', code: <span>Eingabe: <span className="var">Zeit_Start</span></span>},
  {type: 'input', code: <span>Eingabe: <span className="var">Zeit_Ende</span></span>},
  {type: 'step', code: <span><span className="var">Dauer</span> = <span className="var">Zeit_Ende</span> - <span className="var">Zeit_Start</span></span>},
  {type: 'output', code: <span>Ausgabe: <span className="var">Dauer</span></span>}
]} />

<Struktogramm program={[
  {type: 'input', code: <span>Eingabe <span className="var">Temperatur</span></span>},
  {type: 'input', code: <span>Eingabe <span className="var">Wahrscheinlichkeit_Regen</span></span>},
  {type: 'if', code: <span>Wenn <span className="var">Temperatur</span> &gt; <span className="var">30</span>:</span>, block: [
      {type: 'output', code: <span>Ausgabe: Sonnencreme verwenden</span>},
      {type: 'output', code: <span>Ausgabe: Sonnenbrille tragen</span>},
      {type: 'output', code: <span>Ausgabe: Sonnenhut tragen</span>},
    ]},
    {type: 'elif', code: <span>Sonst, wenn <span className="var">Temperatur</span> &le; <span className="var">30</span> und <span className="var">Temperatur</span> &gt; <span className="var">20</span>:</span>, block: [
      {type: 'output', code: <span>Ausgabe: Pullover anziehen</span>},
      {type: 'if', code: <span>Wenn <span className="var">Wahrscheinlichkeit_Regen</span> &ge; <span className="var">0.5</span>:</span>, block: [
        {type: 'output', code: <span>Ausgabe: Regenjacke anziehen</span>},
        {type: 'output', code: <span>Ausgabe: Schirm mitnehmen</span>},
    ]},
    ]},
  {type: 'else', code: <span>Sonst:</span>, block: [
      {type: 'output', code: <span>Ausgabe: warme Jacke anziehen</span>}
    ]},
]} />

<Struktogramm program={[
  {type: 'input', code: <span>Eingabe: <span className="var">Zeit_Start</span></span>},
  {type: 'input', code: <span>Eingabe: <span className="var">Zeit_Ende</span></span>},
  {type: 'input', code: <span>Eingabe: <span className="var">Liter_Wassser</span></span>},
  {type: 'subprogram', code: <span><span className="var">Dauer_Minuten</span> = Dauer(<span className="var">Zeit_Start</span>, <span className="var">Zeit_Ende</span>)</span>},
  {type: 'step', code: <span><span className="var">Wasserverbrauch</span> = <span className="var">Liter_Wasser</span> / <span className="var">Dauer_Minuten</span></span>},
  {type: 'if', code: <span>Wenn <span className="var">Wasserverbrauch</span> &gt; <span className="var">12</span>:</span>, block: [
    {type: 'step', code: <span>Hausdienst benachrichten</span>},
  ]},
  {type: 'output', code: <span>Ausgabe: <span className="var">Wasserverbrauch</span> l/min</span>}
]} />

---
